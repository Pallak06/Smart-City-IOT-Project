The project uses a two-LAN network connected through a central router.
Each LAN has a switch that connects PCs, and the router allows communication between the two networks.
All devices use static IP addresses, and the router performs routing so computers in different LANs can communicate with each other.
